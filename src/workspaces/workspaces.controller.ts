import { Body, Controller, Delete, Get, Param, Patch, Post, UseGuards } from '@nestjs/common'; import { ApiBearerAuth, ApiTags } from '@nestjs/swagger'; import { CurrentUser, JwtAuthGuard, JwtUser } from '../common/auth.js'; import { CreateWorkspaceDto, UpdateWorkspaceDto } from './dto.js'; import { WorkspacesService } from './workspaces.service.js';
@ApiTags('Workspaces') @ApiBearerAuth() @UseGuards(JwtAuthGuard) @Controller('workspaces') export class WorkspacesController { constructor(private readonly service: WorkspacesService) {} @Post() create(@CurrentUser() u: JwtUser, @Body() d: CreateWorkspaceDto) { return this.service.create(u.id, d); } @Get() list(@CurrentUser() u: JwtUser) { return this.service.list(u.id); } @Get(':id') get(@CurrentUser() u: JwtUser, @Param('id') id: string) { return this.service.get(id, u.id); } @Patch(':id') update(@CurrentUser() u: JwtUser, @Param('id') id: string, @Body() d: UpdateWorkspaceDto) { return this.service.update(id, u.id, d); } @Delete(':id') remove(@CurrentUser() u: JwtUser, @Param('id') id: string) { return this.service.remove(id, u.id); } }
